# 直接交给 Codex 的实施指令

把本包全部文件放进项目根目录，然后复制以下内容。必须让编码工具能实际读取整个目录，而不是只收到一句项目名称。

```text
请根据当前仓库的 PharmaScope 文档包实现“医药研发情报与临床试验进展研究平台”V1。

本次授权模式：end-to-end。先实际读取 AGENTS.md、README.md、docs/00-baseline.md、
docs/14-implementation-plan.md 和 delivery/STATE.md，再按任务读取其余规格、契约和测试资料。
先检查当前目录，保留已有文件；不要假定这是 DeerFlow 原仓库，也不要覆盖既有源码。

默认按文档冻结的方案实现：Next.js/React/TypeScript 前端；FastAPI/Python 模块化后端；
PostgreSQL；通过项目适配层接入固定提交的 DeerFlow 2.x harness；开发环境 Docker Compose。
先完成上游兼容性探针并填真实 commit SHA，再完成后端与交接包，然后开发前端和联调。
每次新增/修改前端页面前，重新读取当前 design.md、共享组件、样式和相关接口合同。

实现范围包括真实数据模型/迁移、登录与workspace权限、药物实体审核、试验/文献同步与快照、
字段差异、事件版本、研究任务和SSE、证据和结论、报告人工审核/发布、订阅和可靠投递、
管理页、演示种子、自动化测试和启动部署说明。
ClinicalTrials.gov 与 PubMed 是 V1 必需真实适配器；openFDA/ToolUniverse/全文PDF不是V1阻塞项。

必须同时提供：无需外网密钥的demo闭环，以及显式配置后的live接入路径。
demo的回放不等于真实Agent；缺模型密钥或联网失败时，在交付记录中标记未验证，不可伪装live成功。
不要把文档中的虚构临床数据转成真实结论；不要从原型图抄药物批准日期或疗效数字。

按阶段自行拆任务、编码、测试、修复并推进；不要每完成一个小步骤就等待我的确认。
遇到不可逆删除、付费资源、真实敏感信息或生产发布时停止相关动作，记录阻塞；继续可独立完成部分。
中途会话结束前更新 delivery/STATE.md，确保新会话可根据磁盘文件接续。

最终交付：可运行源码、依赖锁、数据库迁移、启动命令、演示账户说明、完整前后端页面、
真实API与类型生成、测试命令和结果、安全/故障场景证据、已知限制以及 BACKEND_HANDOFF.md。
不要只给项目骨架、静态页面、TODO列表或对未来工作的承诺。
```

## 逐阶段验收方式
将上面 `end-to-end` 改为 `stage-gated`，并加上：“每个里程碑完成后停止，提供验收证据，等我决定进入下一阶段还是修改。”

## 新会话恢复
```text
继续实现当前 PharmaScope 项目。先读 AGENTS.md 和 delivery/STATE.md，再检查 Git 状态、
当前阶段的规格和上次测试结果。以磁盘事实为准，从未完成任务继续，不重建已有模块。
保持此前记录的执行模式；不要把未运行测试改成通过。
```
